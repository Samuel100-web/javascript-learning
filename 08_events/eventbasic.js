/*
const startInterval = function(str){
        console.log(`${str}`, Date.now().toLocaleString())
    }
    document.querySelector('#start').addEventListener('click', function(str){
        setInterval(startInterval,1000, 'Samuel and Sialtiel')
        // console.log('Start Interval')
    })

    // const stopInterval = function(str2){
    //     console.log(`${str2}`)
    // }
    // document.querySelector('#stop').addEventListener('click', function(str2){
    //     clearInterval(stopInterval, 'Stoped Interval')        
    // })

    //const intervalId = document.querySelector('#start')
    const stopInterval = function(message) {
            console.log(message);
            clearInterval(startInterval); // Stop the interval using the interval ID
        }

        // Add event listener to the button
        document.querySelector('#stop').addEventListener('click', function() {
            stopInterval; 
            console.log('stoped Interval')// Call the stopInterval function with a message
        });
*/